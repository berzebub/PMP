# PMP - Project Management Platform Specification

## 1. Overview

PMP เป็นแพลตฟอร์มบริหารจัดการโปรเจคและทรัพยากรบุคคลสำหรับองค์กร พัฒนาด้วย **Vue 3** + **Quasar Framework** โดยใช้ **Firebase** เป็น Backend (Authentication, Firestore, Storage) และ **Pinia** สำหรับ State Management รองรับภาษาไทยเป็นหลัก ใช้ธีม Dark Mode
 
### Tech Stack

| เทคโนโลยี | เวอร์ชัน | หน้าที่ |
|---|---|---|
| Vue 3 | ^3.4.18 | Frontend framework (Composition API) |
| Quasar | ^2.16.0 | UI component framework |
| Pinia | ^3.0.1 | State management |
| Firebase | ^12.4.0 | Auth, Firestore DB, Storage |
| Vue Router | ^4.0.0 | SPA routing |
| xlsx | ^0.18.5 | Excel export |
| @vueuse/core | ^13.9.0 | Vue composition utilities |
| axios | ^1.2.1 | HTTP client |

---

## 2. Authentication & User Management

### 2.1 Login / Register

- **Login** (`/login`): Email + Password ผ่าน Firebase Authentication
- **Register** (`/register`): Email + Password + Confirm Password
- เมื่อ Login สำเร็จ ระบบจะตรวจสอบ Firestore collection `profiles` หาก doc ยังไม่มีจะสร้างให้อัตโนมัติ

### 2.2 Profile

Firestore collection: `profiles` (document ID = email)

| Field | Type | Description |
|---|---|---|
| `firstName` | string | ชื่อ |
| `lastName` | string | นามสกุล |
| `email` | string | อีเมล (จาก Firebase Auth) |
| `role` | string | สิทธิ์ (`employee` / `head` / `hr` / `super_admin`), default: `employee` |
| `department` | string | แผนกที่สังกัด |
| `photoURL` | string | URL รูปโปรไฟล์ (เก็บใน Firebase Storage path: `avatars/{email}`) |
| `createdAt` | Date | วันที่สร้าง |
| `updatedAt` | Date | วันที่อัปเดตล่าสุด |

### 2.3 Profile Management (ผ่าน Dialog ใน MainLayout)

- อัปโหลดรูปโปรไฟล์ (ขนาดไม่เกิน 2MB)
- แก้ไขชื่อ-นามสกุล
- แสดง Role และ Department (อ่านอย่างเดียว)
- เปลี่ยนรหัสผ่าน (ซ่อนอยู่ กดเปิดเมื่อต้องการ):
  - กรอกรหัสผ่านเดิม, รหัสผ่านใหม่ (ขั้นต่ำ 6 ตัวอักษร), ยืนยันรหัสผ่าน
- Avatar fallback: หากไม่มีรูป แสดงตัวอักษรแรกของ email

### 2.4 Role-Based Access Control (RBAC)

| Role | Label | Icon | สิทธิ์ |
|---|---|---|---|
| `super_admin` | Super Admin | `shield` | จัดการผู้ใช้, อนุมัติลาแทน Head ทุกแผนก, อนุมัติลาแทน HR, เข้าถึง Admin Panel |
| `hr` | HR | `badge` | อนุมัติลา (ขั้น HR), จัดการเบิกค่าใช้จ่าย, ดูข้อมูลทุกคน |
| `head` | หัวหน้าแผนก | `supervisor_account` | อนุมัติลาของพนักงานในแผนกเดียวกัน |
| `employee` | พนักงาน | `person` | ใช้งานฟีเจอร์พื้นฐานทั้งหมด |

### 2.5 Admin Panel (`/admin`) -- เฉพาะ `super_admin`

**Tab จัดการผู้ใช้:**

- ค้นหาผู้ใช้ด้วย email หรือชื่อ
- แสดงรายชื่อผู้ใช้ทั้งหมดพร้อม: Avatar, ชื่อ-นามสกุล, email, Role ปัจจุบัน
- แสดงโปรเจคที่ผู้ใช้แต่ละคนสังกัด (chips)
- แก้ไข Role และ แผนก ของแต่ละคนได้

**Tab จัดการแผนก:**

- เพิ่ม / แก้ไข / ลบ แผนก
- แสดงจำนวนพนักงานในแต่ละแผนก
- แจ้งเตือนก่อนลบแผนกที่มีคนสังกัด

---

## 3. Project Management

### 3.1 Dashboard / Home (`/`)

- แสดงรายชื่อโปรเจคที่ผู้ใช้เป็นสมาชิก
- แสดง: ชื่อโปรเจค, คำอธิบาย, จำนวนสมาชิก
- สร้างโปรเจคใหม่ (ชื่อ + คำอธิบาย)
- กดเข้าโปรเจคเพื่อไปหน้า Project Page

### 3.2 Project Data Model

Firestore collection: `projects`

| Field | Type | Description |
|---|---|---|
| `name` | string | ชื่อโปรเจค |
| `description` | string | คำอธิบาย |
| `members` | string[] | รายชื่อ email ของสมาชิก |
| `createdBy` | string | email ผู้สร้าง |
| `createdAt` | Date | วันที่สร้าง |
| `updatedAt` | Date | วันที่อัปเดต |

### 3.3 Project Page (`/project/:id`) -- 3 มุมมอง

#### 3.3.1 Kanban Board

- **Columns (Statuses)**: ปรับแต่งได้ -- เพิ่ม/แก้ไข/ลบ/เรียงลำดับ
- Default columns: `To Do`, `In Progress`, `Review`, `Done`
- แต่ละ Column มี: ชื่อ, สีกำกับ, ลำดับ
- **Drag & Drop**: ลากย้าย Task ระหว่าง Column และเรียงลำดับภายใน Column
- **Task Card** แสดง: แถบสี Priority, Checkbox เสร็จสิ้น, ชื่อ Task, แถบ Subtask progress, Due date, Avatar ผู้รับมอบหมาย
- **สร้าง Task** ผ่าน Dialog: ชื่อ, คำอธิบาย, Priority, วันกำหนดส่ง, ผู้รับมอบหมาย

#### 3.3.2 Calendar View

- แสดงปฏิทินรายเดือนพร้อมจุดสีสถานะ Task ที่มี Due date
- กดดูรายละเอียด Task ในแต่ละวัน
- สร้าง Task จากวันที่ในปฏิทินได้

#### 3.3.3 Gantt Chart

- แสดง Task เป็นแถบตาม Timeline (เริ่มจาก createdAt ถึง dueDate)
- Zoom: Day / Week / Month
- แสดง Progress จาก Subtask completion
- สีแถบตาม Priority, แถบสีแดงถ้าเลยกำหนด
- เส้น Today indicator, Highlight วันหยุด
- Filter ตามสถานะ

### 3.4 Task Data Model

Firestore collection: `tasks`

| Field | Type | Description |
|---|---|---|
| `title` | string | ชื่อ Task |
| `description` | string | รายละเอียด |
| `status` | string | Column/Status ID |
| `priority` | string | `low` / `medium` / `high` / `urgent` |
| `projectId` | string | ID โปรเจค |
| `createdBy` | string | email ผู้สร้าง |
| `assignedTo` | string / null | email ผู้รับมอบหมาย |
| `dueDate` | Date / null | วันกำหนดส่ง |
| `order` | number | ลำดับใน Column (Drag & Drop) |
| `completed` | boolean | สถานะเสร็จสิ้น |
| `completedAt` | Date / null | วันที่ Mark Complete |
| `isDeleted` | boolean | อยู่ในถังขยะหรือไม่ |
| `deletedAt` | Date / null | วันที่ลบ |
| `deletedBy` | string / null | email ผู้ลบ |
| `createdAt` | Date | วันที่สร้าง |
| `updatedAt` | Date | วันที่อัปเดต |

### 3.5 Subtask

Firestore collection: `subtasks`

| Field | Type | Description |
|---|---|---|
| `title` | string | ชื่อ Subtask |
| `description` | string | รายละเอียด |
| `taskId` | string | ID Task หลัก |
| `projectId` | string | ID โปรเจค |
| `assignedTo` | string / null | ผู้รับมอบหมาย |
| `dueDate` | Date / null | วันกำหนด |
| `completed` | boolean | สถานะเสร็จสิ้น |
| `createdBy` | string | ผู้สร้าง |
| `createdAt` | Date | วันที่สร้าง |
| `updatedAt` | Date | วันที่อัปเดต |

- แก้ไข Inline ได้ (ชื่อ, คำอธิบาย, ผู้รับมอบหมาย, วันกำหนด)
- Toggle Complete จะบันทึกลง Work Log อัตโนมัติ

### 3.6 Task Comments & Mentions

Firestore collection: `comments`

| Field | Type | Description |
|---|---|---|
| `taskId` | string | ID Task |
| `text` | string | เนื้อหาคอมเมนต์ |
| `userEmail` | string | email ผู้เขียน |
| `userName` | string | ชื่อผู้เขียน |
| `createdAt` | Date | วันที่เขียน |
| `updatedAt` | Date | วันที่อัปเดต |

- รองรับ **@mention** ผู้ใช้ -- สร้าง Notification ไปยังผู้ถูก mention
- รองรับ **ลิงก์** ในข้อความ -- คลิกเปิดได้
- Real-time subscription ผ่าน Firestore `onSnapshot`

### 3.7 Task Activity Log

เก็บใน collection `comments` เช่นกัน โดยมี `type: 'activity'`

| Field เพิ่มเติม | Type | Description |
|---|---|---|
| `type` | string | `'activity'` |
| `activityType` | string | `'status_change'` |
| `fromStatus` | string / null | สถานะเดิม |
| `toStatus` | string / null | สถานะใหม่ |

### 3.8 Task Attachments

Firestore collection: `attachments`

Storage path: `attachments/{taskId}/{fileName}`

| Field | Type | Description |
|---|---|---|
| `taskId` | string | ID Task |
| `fileName` | string | ชื่อไฟล์ |
| `fileSize` | number | ขนาดไฟล์ (bytes) |
| `fileType` | string | MIME type |
| `filePath` | string | Path ใน Storage |
| `downloadURL` | string | URL ดาวน์โหลด |
| `uploadedBy` | string | email ผู้อัปโหลด |
| `uploadedByName` | string | ชื่อผู้อัปโหลด |
| `createdAt` | Date | วันที่อัปโหลด |

- ขนาดไฟล์สูงสุด: **100 MB**

### 3.9 Task Completion & Work Log Integration

- เมื่อ **Mark Complete** บน TaskCard จะเพิ่มรายการอัตโนมัติใน Work Log: `✅ เสร็จสิ้น: {ชื่อ Task}`
- เมื่อ **ยกเลิก Complete** จะเพิ่มรายการ: `↩️ ยกเลิกเสร็จสิ้น: {ชื่อ Task}`
- เช่นเดียวกันกับ **Subtask** completion

### 3.10 Trash (`/project/:id/trash`)

- ดู Task ที่ถูกลบ
- Restore Task กลับมา
- ลบถาวร หรือ ล้างถังขยะทั้งหมด

### 3.11 Project Settings (`/project/:id/settings`)

- แก้ไขชื่อ / คำอธิบายโปรเจค
- จัดการสมาชิก: เพิ่ม / ลบ สมาชิก
- ลบโปรเจค (เฉพาะ owner, ต้องพิมพ์ชื่อยืนยัน)

---

## 4. Daily Check-in (`/checkins`)

### 4.1 ระบบ Standup 3 คำถาม (Agile Style)

Firestore collection: `checkins`

| Field | Type | Description |
|---|---|---|
| `userId` | string | email |
| `userName` | string | ชื่อ |
| `date` | string | YYYY-MM-DD |
| `yesterday` | string | เมื่อวานทำอะไร (สูงสุด 500 ตัวอักษร) |
| `today` | string | วันนี้จะทำอะไร (สูงสุด 500 ตัวอักษร) |
| `blockers` | string | อุปสรรค (ไม่บังคับ, สูงสุด 500 ตัวอักษร) |
| `mood` | string | อารมณ์ (emoji) |
| `checkedInAt` | Timestamp | เวลาเช็คชื่อ |

### 4.2 Mood Options

| Emoji | Label |
|---|---|
| 🔥 | มุ่งมั่น |
| 😊 | สดใส |
| 💪 | พร้อมลุย |
| 🧘 | สงบ |
| 😴 | ง่วง |
| 🤒 | ไม่สบาย |

### 4.3 Gamification -- Level System

| Level | Icon | เช็คชื่อขั้นต่ำ |
|---|---|---|
| Newbie | 🌱 | 0 ครั้ง |
| Regular | ⭐ | 5 ครั้ง |
| Committed | 💎 | 15 ครั้ง |
| Dedicated | 🏅 | 30 ครั้ง |
| Champion | 👑 | 60 ครั้ง |

- แสดง Progress bar ไปยัง Level ถัดไป

### 4.4 Gamification -- Streak System

- **Current Streak**: จำนวนวันทำงานติดต่อกันที่เช็คชื่อ
- **Longest Streak**: สถิติ Streak สูงสุด
- วันเสาร์-อาทิตย์ไม่ทำให้ Streak ขาด (แต่เช็คในวันหยุดนับเป็นโบนัส)
- Streak Tiers:
  - >= 30 วัน: 🔥🔥🔥 Legendary
  - >= 7 วัน: 🔥🔥 On Fire
  - >= 1 วัน: 🔥 Started

### 4.5 Gamification -- Badge System

| Badge | Icon | Condition |
|---|---|---|
| First Step | 🎯 | เช็คชื่อครั้งแรก (total >= 1) |
| Week Warrior | ⚔️ | Streak 7 วัน |
| Month Master | 🏆 | Streak 30 วัน |
| Half Century | 💯 | เช็คชื่อ 50 ครั้ง |
| Centurion | 🌟 | เช็คชื่อ 100 ครั้ง |

### 4.6 UI Components

- **Calendar Heatmap**: แสดงวันที่เช็คชื่อบนปฏิทินแบบ GitHub-style
- **Stats Cards**: Current Streak, Longest Streak, Level, This Month Count
- **Team Check-ins**: แสดงรายชื่อทีมที่เช็คชื่อวันนี้
- **Recent History**: ประวัติเช็คชื่อล่าสุด (ขยายดูรายละเอียดได้)

---

## 5. Work Log (`/worklogs`)

### 5.1 Data Model

Firestore collection: `worklogs`

| Field | Type | Description |
|---|---|---|
| `userId` | string | email |
| `userName` | string | ชื่อ |
| `date` | string | YYYY-MM-DD |
| `entries` | string[] | รายการงานที่ทำ (อย่างน้อย 1 รายการ) |
| `summary` | string | สรุปภาพรวม (ไม่บังคับ, สูงสุด 300 ตัวอักษร) |
| `submittedAt` | Timestamp | เวลาส่งครั้งแรก |
| `updatedAt` | Timestamp | เวลาอัปเดตล่าสุด |

### 5.2 Features

- กรอกรายการงานได้หลายรายการ (เพิ่ม/ลบ dynamic rows)
- Enter สร้างรายการใหม่, Backspace ลบรายการว่าง
- แก้ไข Log ของวันนี้ได้ (อัปเดตทับ)
- แจ้งเตือนหลัง 17:30 ว่าควรส่ง Work Log
- **Auto-entry**: เมื่อ Mark Complete Task/Subtask ระบบเพิ่มรายการอัตโนมัติ
- **ประวัติส่วนตัว**: ย้อนหลัง 30 วัน
- **ประวัติทีม**: ดูตามวันที่ที่เลือก

---

## 6. Leave Request (`/leaves`)

### 6.1 ประเภทการลา

| Value | Label | Icon | หมายเหตุ |
|---|---|---|---|
| `sick` | ลาป่วย | 🤒 | อนุมัติอัตโนมัติ |
| `personal` | ลากิจ | 📋 | ต้องผ่านการอนุมัติ |
| `vacation` | ลาพักร้อน | 🏖️ | ต้องผ่านการอนุมัติ |

### 6.2 Approval Workflow

```
[พนักงานส่งคำขอ]
    │
    ├── ลาป่วย ──────────────────────► [approved] อนุมัติอัตโนมัติ
    │
    ├── พนักงานทั่วไป ──► [pending_head] รอหัวหน้าอนุมัติ
    │                         │
    │                         ├── หัวหน้าอนุมัติ ──► [pending_hr] รอ HR อนุมัติ
    │                         │                          │
    │                         │                          ├── HR อนุมัติ ──► [approved]
    │                         │                          └── HR ไม่อนุมัติ ──► [rejected]
    │                         │
    │                         ├── หัวหน้าไม่อนุมัติ ──► [rejected]
    │                         └── ผู้ขอยกเลิก ──► [cancelled]
    │
    └── หัวหน้า/Super Admin ──► [pending_hr] (ข้ามขั้น Head)
                                    │
                                    ├── HR อนุมัติ ──► [approved]
                                    ├── HR ไม่อนุมัติ ──► [rejected]
                                    └── ผู้ขอยกเลิก ──► [cancelled]
```

### 6.3 สิทธิ์การอนุมัติ

| สถานะ | ผู้มีสิทธิ์อนุมัติ | เงื่อนไข |
|---|---|---|
| `pending_head` | Head | เฉพาะพนักงานในแผนกเดียวกัน |
| `pending_head` | Super Admin | ทุกแผนก (ทำหน้าที่แทน Head) |
| `pending_hr` | HR | ทุกแผนก |
| `pending_hr` | Super Admin | ทุกแผนก (ทำหน้าที่แทน HR) |

- Head ลาเอง → ส่งตรงไป `pending_hr`
- Super Admin ลาเอง → ส่งตรงไป `pending_hr`

### 6.4 Leave Data Model

Firestore collection: `leaves`

| Field | Type | Description |
|---|---|---|
| `userId` | string | email ผู้ขอ |
| `userName` | string | ชื่อ |
| `firstName` | string | ชื่อจริง |
| `lastName` | string | นามสกุล |
| `leaveType` | string | ประเภทการลา |
| `startDate` | string | วันเริ่มลา (YYYY-MM-DD) |
| `endDate` | string | วันสิ้นสุด (YYYY-MM-DD) |
| `details` | string | เหตุผล |
| `department` | string | แผนก |
| `status` | string | สถานะ |
| `submittedAt` | Timestamp | วันที่ส่ง |
| `updatedAt` | Timestamp | วันที่อัปเดต |
| `headApproval` | object | `{ approvedBy, approvedByName, approvedAt, comment }` |
| `hrApproval` | object | `{ approvedBy, approvedByName, approvedAt, comment }` |
| `rejectedBy` | string | email ผู้ปฏิเสธ |
| `rejectionReason` | string | เหตุผลที่ปฏิเสธ |

### 6.5 Status Labels

| Status | Label | Color | Icon |
|---|---|---|---|
| `pending_head` | รอหัวหน้าอนุมัติ | #ffb74d | `hourglass_empty` |
| `pending_hr` | รอ HR อนุมัติ | #ce93d8 | `hourglass_top` |
| `approved` | อนุมัติแล้ว | #66bb6a | `check_circle` |
| `rejected` | ไม่อนุมัติ | #ef5350 | `cancel` |
| `cancelled` | ยกเลิกแล้ว | #9e9e9e | `block` |

### 6.6 Features เพิ่มเติม

- **Export Excel**: ส่งออกประวัติการลา (ของตัวเอง หรือ ทั้งทีม)
- **Refresh**: ปุ่มดึงข้อมูลใหม่ทั้งส่วนรออนุมัติ และประวัติ
- **Today Marker**: ใน "ทั้งทีม" แสดง badge "วันนี้" สำหรับรายการที่กำลังลาอยู่ในวันนี้
- **ยกเลิกคำขอ**: ผู้ขอสามารถยกเลิกได้เมื่อสถานะยังเป็น pending

---

## 7. Expense Reimbursement (`/expenses`)

### 7.1 Approval Workflow

```
[พนักงานส่งคำขอเบิก]
    │
    ▼
[pending_hr] รอ HR ตรวจสอบ
    │
    ├── HR สั่งพิมพ์เอกสาร ──► ส่งให้ CEO เซ็นจริง (กระดาษ)
    │
    ├── HR กด "อนุมัติ" (หลัง CEO เซ็น) ──► [approved] CEO อนุมัติแล้ว
    │       │
    │       ▼
    │   HR จ่ายเงิน (โอน/เงินสด) ──► [paid] จ่ายเงินแล้ว
    │
    ├── HR ไม่อนุมัติ ──► [rejected]
    │
    └── ผู้ขอยกเลิก ──► [cancelled]
```

### 7.2 Expense Data Model

Firestore collection: `expenses`

| Field | Type | Description |
|---|---|---|
| `userId` | string | email ผู้ขอ |
| `userName` | string | ชื่อ |
| `firstName` | string | ชื่อจริง |
| `lastName` | string | นามสกุล |
| `department` | string | แผนก |
| `items` | array | รายการเบิก |
| `items[].seq` | number | ลำดับ |
| `items[].description` | string | รายการ |
| `items[].amount` | number | จำนวนเงิน |
| `items[].receiptURL` | string | URL ใบเสร็จ (ถ้าแนบ) |
| `totalAmount` | number | ยอดรวม (คำนวณอัตโนมัติ) |
| `note` | string | หมายเหตุ |
| `status` | string | สถานะ |
| `submittedAt` | Timestamp | วันที่ส่ง |
| `updatedAt` | Timestamp | วันที่อัปเดต |
| `hrReviewedBy` | string | email HR ผู้ตรวจ |
| `hrReviewedByName` | string | ชื่อ HR |
| `hrReviewedAt` | Timestamp | วันที่ตรวจ |
| `ceoApprovedAt` | Timestamp | วันที่ CEO อนุมัติ |
| `ceoMarkedBy` | string | email ผู้กดอนุมัติ |
| `paymentMethod` | string | วิธีจ่ายเงิน (`transfer` / `cash`) |
| `paymentSlipURL` | string | URL สลิปโอนเงิน |
| `paymentNote` | string | หมายเหตุการจ่าย |
| `paidAt` | Timestamp | วันที่จ่ายเงิน |
| `paidBy` | string | email ผู้จ่าย |
| `paidByName` | string | ชื่อผู้จ่าย |
| `rejectedBy` | string | email ผู้ปฏิเสธ |
| `rejectedByName` | string | ชื่อผู้ปฏิเสธ |
| `rejectionReason` | string | เหตุผลที่ปฏิเสธ |
| `rejectedAt` | Timestamp | วันที่ปฏิเสธ |

### 7.3 Status Labels

| Status | Label | Color | Icon |
|---|---|---|---|
| `pending_hr` | รอ HR ตรวจสอบ | #ce93d8 | `hourglass_empty` |
| `approved` | CEO อนุมัติแล้ว | #42a5f5 | `verified` |
| `paid` | จ่ายเงินแล้ว | #66bb6a | `check_circle` |
| `rejected` | ไม่อนุมัติ | #ef5350 | `cancel` |
| `cancelled` | ยกเลิกแล้ว | #9e9e9e | `block` |

### 7.4 Features

- **ฟอร์มเบิก**: กรอกหลายรายการ (ลำดับ, รายการ, จำนวนเงิน), แนบใบเสร็จต่อรายการ, หมายเหตุ, คำนวณยอดรวมอัตโนมัติ
- **พิมพ์เอกสาร**: HR สั่งพิมพ์ใบขออนุมัติ (ข้อมูลบริษัท, ผู้ขอ, ตารางรายการ, ช่องลายเซ็น) -- เปิดหน้าต่างใหม่, บังคับ Light mode
- **การอนุมัติ**: มี Confirmation dialog ก่อนกดอนุมัติ ป้องกันกดผิด
- **การจ่ายเงิน**: เลือกวิธี (โอน/เงินสด), อัปโหลดสลิปโอน หรือ บันทึกจ่ายเงินสด
- **Export Excel**: ส่งออกประวัติ (ของตัวเอง / ทั้งหมด)
- **Refresh**: ปุ่มดึงข้อมูลใหม่ทั้งส่วนจัดการ และประวัติ
- **ดูรายละเอียด**: กดที่รายการในประวัติเพื่อดู Dialog รายละเอียดเต็ม

---

## 8. Notification System

### 8.1 Data Model

Firestore collection: `notifications`

| Field | Type | Description |
|---|---|---|
| `recipientEmail` | string | email ผู้รับ |
| `type` | string | `mention` / `assign` / `task_update` |
| `title` | string | หัวข้อ |
| `message` | string | เนื้อหา |
| `projectId` | string / null | ID โปรเจค |
| `projectName` | string / null | ชื่อโปรเจค |
| `taskId` | string / null | ID Task |
| `taskTitle` | string / null | ชื่อ Task |
| `senderEmail` | string | email ผู้ส่ง |
| `senderName` | string | ชื่อผู้ส่ง |
| `read` | boolean | อ่านแล้ว/ยัง |
| `createdAt` | Timestamp | วันที่สร้าง |

### 8.2 Trigger Events

| Event | Notification Type | ผู้รับ |
|---|---|---|
| สร้าง Task แล้วมอบหมาย | `assign` | ผู้ถูกมอบหมาย |
| แก้ไขผู้รับมอบหมาย Task | `assign` | ผู้รับมอบหมายใหม่ |
| @mention ใน Comment | `mention` | ผู้ถูก mention |

### 8.3 UI

- Bell icon ใน Header พร้อม Badge จำนวนที่ยังไม่อ่าน
- Notification Panel: แสดงรายการ, icon ตามประเภท, ชื่อโปรเจค, เวลาสัมพัทธ์
- กดที่แจ้งเตือน: Mark as read + นำทางไปยังโปรเจค/Task ที่เกี่ยวข้อง
- ปุ่ม "อ่านทั้งหมด" (Batch mark all as read)
- กดนอกพื้นที่เพื่อปิด Panel

---

## 9. Company Calendar (`/calendar`)

- แสดง **Google Calendar** แบบฝัง (iframe)
- แสดงปฏิทินบริษัทจาก Google Calendar ที่กำหนดไว้
- อ่านอย่างเดียว (ไม่สามารถสร้าง Event ผ่านระบบ)

---

## 10. Navigation & Layout

### 10.1 Main Layout

- **Header**: โลโก้ "Edutech Innovation", ปุ่ม Daily Check-in (แสดง Streak), ปุ่ม Notification, Avatar dropdown (Profile / Logout)
- **Drawer (ซ้าย)**: เมนูหลักทั้งหมด
- **Responsive**: รองรับ Mobile

### 10.2 Menu Structure

| เมนู | Path | Icon | สี | เงื่อนไข | Badge |
|---|---|---|---|---|---|
| โปรเจค (รายการ) | `/project/:id` | `folder` | -- | -- | จำนวนโปรเจค |
| Daily Check-in | `/checkins` | `event_available` | #66bb6a | -- | แจ้งเตือนถ้ายังไม่ Check-in |
| ขอลา | `/leaves` | `event_busy` | #ffb74d | -- | -- |
| เบิกค่าใช้จ่าย | `/expenses` | `receipt_long` | #26a69a | -- | -- |
| บันทึกการทำงาน | `/worklogs` | `edit_note` | #ab47bc | -- | แจ้งเตือนถ้ายังไม่ส่ง |
| ปฏิทินบริษัท | `/calendar` | `calendar_month` | #42a5f5 | -- | -- |
| Admin Panel | `/admin` | `admin_panel_settings` | #ef5350 | `super_admin` เท่านั้น | -- |

---

## 11. Routes Summary

### Public Routes

| Path | Page | Description |
|---|---|---|
| `/login` | LoginPage | หน้า Login |
| `/register` | RegisterPage | หน้าสมัครสมาชิก |

### Protected Routes (ต้อง Login)

| Path | Page | Description |
|---|---|---|
| `/` | IndexPage | Dashboard แสดงโปรเจค |
| `/checkins` | CheckinPage | เช็คชื่อรายวัน |
| `/worklogs` | WorklogPage | บันทึกการทำงาน |
| `/leaves` | LeavePage | ขอลา |
| `/expenses` | ExpensePage | เบิกค่าใช้จ่าย |
| `/calendar` | CalendarPage | ปฏิทินบริษัท |
| `/admin` | AdminPage | Admin Panel |
| `/project/:id` | ProjectPage | หน้าโปรเจค |
| `/project/:id/settings` | ProjectSettingsPage | ตั้งค่าโปรเจค |
| `/project/:id/trash` | TrashPage | ถังขยะโปรเจค |

### Fallback

| Path | Page | Description |
|---|---|---|
| `/:catchAll(.*)*` | ErrorNotFound | หน้า 404 |

---

## 12. Firestore Collections Summary

| Collection | หน้าที่ | Document ID |
|---|---|---|
| `profiles` | ข้อมูลผู้ใช้ | email |
| `departments` | แผนก | auto-generated |
| `projects` | โปรเจค | auto-generated |
| `tasks` | Task | auto-generated |
| `subtasks` | Subtask | auto-generated |
| `comments` | Comment + Activity | auto-generated |
| `attachments` | ไฟล์แนบ Task | auto-generated |
| `checkins` | เช็คชื่อรายวัน | auto-generated |
| `worklogs` | บันทึกการทำงาน | auto-generated |
| `leaves` | คำขอลา | auto-generated |
| `expenses` | คำขอเบิกค่าใช้จ่าย | auto-generated |
| `notifications` | การแจ้งเตือน | auto-generated |

---

## 13. Firebase Storage Paths

| Path | หน้าที่ | ขนาดสูงสุด |
|---|---|---|
| `avatars/{email}` | รูปโปรไฟล์ | 2 MB |
| `attachments/{taskId}/{fileName}` | ไฟล์แนบ Task | 100 MB |
| `expenses/{expenseId}/receipts/{seq}_{fileName}` | ใบเสร็จค่าใช้จ่าย | -- |
| `expenses/{expenseId}/payment_slip_{fileName}` | สลิปโอนเงิน | -- |
